export function normalize(s){return String(s||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();}
export function localToday(now=new Date()){return new Intl.DateTimeFormat('en-CA',{timeZone:'America/Tijuana',year:'numeric',month:'2-digit',day:'2-digit'}).format(now);}
export function addDays(date,n){const d=new Date(date+'T12:00:00Z');d.setUTCDate(d.getUTCDate()+n);return d.toISOString().slice(0,10);}
export function occurs(e,from,to=from){if(e.sessions?.length)return e.sessions.some(s=>s.date>=from&&s.date<=to);return e.date<=to&&(e.endDate||e.date)>=from;}
export function nextDate(e,today){return e.sessions?.map(s=>s.date).filter(d=>d>=today).sort()[0]||(e.date<today&&(e.endDate||e.date)>=today?today:e.date);}
export function filterEvents(events,{q='',category='Todo',venue='',date='upcoming',free=false,budget='',day='',today=localToday()}={}){return events.filter(e=>{if(!normalize([e.title,e.venue,e.category,e.description].join(' ')).includes(normalize(q)))return false;if(category!=='Todo'&&category!==e.category)return false;if(venue&&e.venue!==venue)return false;if(free&&!e.free)return false;if(budget&&(typeof e.priceValue!=='number'||e.priceValue>Number(budget)))return false;if(day)return occurs(e,day);if(date==='all')return true;if(date==='today')return occurs(e,today);if(date==='week')return occurs(e,today,addDays(today,6));if(date==='month'){const end=new Date(today.slice(0,7)+'-01T12:00:00Z');end.setUTCMonth(end.getUTCMonth()+1);end.setUTCDate(0);return occurs(e,today,end.toISOString().slice(0,10));}return e.sessions?.length?e.sessions.some(s=>s.date>=today):(e.endDate||e.date)>=today;}).sort((a,b)=>nextDate(a,today).localeCompare(nextDate(b,today))||a.title.localeCompare(b.title,'es'));}
